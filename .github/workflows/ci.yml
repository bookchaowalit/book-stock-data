name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      FREE_ONLY: "true"
      ALLOW_PAID_PROVIDERS: "false"
      ALLOW_EXTERNAL_WRITES: "false"
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: python -m pip install -e .
      # Full Bronze/Silver integration runs from solo-empire, where the shared
      # data_lake adapter is available. This public repo CI owns standalone
      # contract, policy, and compile checks; lake tests skip transparently
      # when the private monorepo runtime is not present.
      - run: python -m unittest discover -s tests -v
      - run: python -m compileall -q src
      - run: python -m book_stock.ingest --help
      - run: book-stock-data --help
